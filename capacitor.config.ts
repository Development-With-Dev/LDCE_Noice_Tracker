import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'it.piemonte.arpa.openoise',
  appName: 'LDCE Noise Tracker',
  webDir: 'www'
};

export default config;
